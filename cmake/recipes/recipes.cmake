# --- recipes.cmake
set(ABSL_RECIPE_DIR "${LITERTLM_RECIPES_DIR}/absl" CACHE PATH "Path to Abseil-cpp related build scrips")

set(FLATBUFFERS_RECIPE_DIR "${LITERTLM_RECIPES_DIR}/flatbuffers" CACHE PATH "Path to Flatbuffers related build scrips")

set(LITERT_RECIPE_DIR "${LITERTLM_RECIPES_DIR}/litert" CACHE PATH "Path to LiteRT related build scrips")

set(PROTOBUF_RECIPE_DIR "${LITERTLM_RECIPES_DIR}/protobuf" CACHE PATH "Path to Protobuf related build scrips")

set(SENTENCEPIECE_RECIPE_DIR "${LITERTLM_RECIPES_DIR}/sentencepiece" CACHE PATH "Path to SentencePiece related build scrips")

set(TFLITE_RECIPE_DIR "${LITERTLM_RECIPES_DIR}/tflite" CACHE PATH "Path to TFLite related build scrips")

# set(ABSL_RECIPE_DIR "${LITERTLM_RECIPES_DIR}/absl" CACHE PATH "Path to Abseil-cpp related build scrips")

# set(ABSL_RECIPE_DIR "${LITERTLM_RECIPES_DIR}/absl" CACHE PATH "Path to Abseil-cpp related build scrips")

# set(ABSL_RECIPE_DIR "${LITERTLM_RECIPES_DIR}/absl" CACHE PATH "Path to Abseil-cpp related build scrips")
